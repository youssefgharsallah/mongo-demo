kind: Service
apiVersion: v1
metadata:
  name: app-service
spec:
  selector:
    app: app-service
  ports:
    - protocol: TCP
      port: 8080
      nodePort: 30181
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-service
spec:
  selector:
    matchLabels:
      app: app-service
  replicas: 1
  template:
    metadata:
      labels:
        app: app-service
    spec:
      containers:
        - name: app-service
          image: alidaoud/mongo-demo:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: MONGO_DB
              valueFrom:
                configMapKeyRef:
                  name: mongo-demo-db-config
                  key: database
            - name: MONGO_HOST
              valueFrom:
                configMapKeyRef:
                  name: mongo-demo-db-config
                  key: host